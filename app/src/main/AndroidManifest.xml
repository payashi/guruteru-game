<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.websarva.wings.android.vs_teruteru">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/guruteru_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/guruteru_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HowToActivity"></activity>
        <activity android:name=".GameClearActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".OpeningActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>